import React from "react";

export default function MediaPlayer() {
    return (
        <div style={{
            width: "100%",
            height: 360,
            background: "#111",
            display: "flex",
            justifyContent: "center",
            alignItems: "center"
        }}>
            <h3>Media Player (YouTube / ScreenShare later)</h3>
        </div>
    );
}
